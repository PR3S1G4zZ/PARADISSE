# Same-origin API. Leave unset/empty so the browser calls relative /api.
# Dev: Vite proxies /api → http://localhost:3001 (or API_UPSTREAM).
# Prod: Caddy proxies /api → API_UPSTREAM. Do not bake an absolute backend URL.
# VITE_API_URL=
